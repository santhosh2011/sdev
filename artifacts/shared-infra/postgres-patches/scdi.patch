--- a/core/scdi/docker-compose.tmpl
+++ b/core/scdi/docker-compose.tmpl
@@ -1,39 +1,8 @@
-# Per-task SCDI stack — generated by bin/new-task for project sdi.
-# Migrated onto the new NVIDIA repos:
-#   api  -> edm-apps-scdi-backend   (worktree dir: cognito_ai_scdi_api)
-#   ui   -> edm-apps-ai-observability (vite app lives in cognitoaitesting/)
-#
-# api/worker/beat are BUILT per task from the backend's own cicd/Dockerfile
-# (multi-stage, targets api|worker|beat). The image installs msodbcsql18 which
-# is amd64-only, so we pin platform: linux/amd64 (runs under emulation on Apple
-# Silicon / OrbStack). The heavy builder + base layers are shared across the
-# three targets, so a single `compose build` builds them once.
-#
-# DB is a self-contained postgres container (develop migrated MySQL -> Postgres,
-# 2026-07; creds match e2e/docker-compose.e2e.yml); alembic builds the schema on
-# api startup. No seed SQL, no remote infra (local profile). Redis is the broker.
-#
-# Host ports come from .env (<SVC>_HOST_PORT). App config comes from ./app.env
-# (symlink -> confs/cognito-ai-scdi-api.<profile>.env).
-
+# Per-task SCDI stack, snapshotted by sdev new for the local profile.
+# Postgres 18 is supplied by sdev infra; dbinit creates this workspace database.
+# The API runs Alembic on startup; Redis and Nginx stay workspace-local.
+# Application sources, build targets and other configuration are unchanged.
 services:
-  postgres:
-    image: postgres:16
-    restart: unless-stopped
-    environment:
-      POSTGRES_USER: scdi
-      POSTGRES_PASSWORD: scdipass
-      POSTGRES_DB: scdi
-    ports:
-      - "127.0.0.1:${DB_HOST_PORT}:5432"
-    volumes:
-      - scdi-postgres:/var/lib/postgresql/data
-    healthcheck:
-      test: ["CMD-SHELL", "pg_isready -U scdi -d scdi"]
-      interval: 5s
-      timeout: 5s
-      retries: 20
-    networks: [task-net]
   redis:
     image: redis:7-alpine
     restart: unless-stopped
@@ -57,8 +26,12 @@
       - ./app.env
       - ./.env
     environment:
-      DB_HOST: postgres
+      DB_HOST: ${DB_HOST}
       REDIS_URL: redis://redis:6379/0
+      DB_PORT: ${DB_PORT}
+      DB_USER: ${DB_USER}
+      DB_PASSWORD: ${DB_PASSWORD}
+      DB_NAME: ${DB_NAME}
     ports:
       - "127.0.0.1:${API_HOST_PORT}:8291"
     volumes:
@@ -72,10 +45,10 @@
       - ./cognito_ai_scdi_api/manage.py:/app/manage.py
       - ./cognito_ai_scdi_api/scripts:/app/scripts
     depends_on:
-      postgres:
-        condition: service_healthy
       redis:
         condition: service_started
+      dbinit:
+        condition: service_completed_successfully
     command:
       - sh
       - -c
@@ -87,7 +60,7 @@
           echo "[entrypoint] APP_ENV=$$APP_ENV (non-local) -> skipping alembic migrations"
         fi
         exec uvicorn scdi_api.api.main:app --host 0.0.0.0 --port 8291 --reload
-    networks: [task-net]
+    networks: [task-net, sdev-shared]
   worker:
     build:
       context: ./cognito_ai_scdi_api
@@ -101,19 +74,23 @@
       - ./app.env
       - ./.env
     environment:
-      DB_HOST: postgres
+      DB_HOST: ${DB_HOST}
       REDIS_URL: redis://redis:6379/0
+      DB_PORT: ${DB_PORT}
+      DB_USER: ${DB_USER}
+      DB_PASSWORD: ${DB_PASSWORD}
+      DB_NAME: ${DB_NAME}
     volumes:
       - ./cognito_ai_scdi_api/src:/app/src
       - ./cognito_ai_scdi_api/configs:/app/configs
       - ./cognito_ai_scdi_api/manage.py:/app/manage.py
       - ./cognito_ai_scdi_api/scripts:/app/scripts
     depends_on:
-      postgres:
-        condition: service_healthy
       redis:
         condition: service_started
-    networks: [task-net]
+      dbinit:
+        condition: service_completed_successfully
+    networks: [task-net, sdev-shared]
   beat:
     build:
       context: ./cognito_ai_scdi_api
@@ -127,8 +104,12 @@
       - ./app.env
       - ./.env
     environment:
-      DB_HOST: postgres
+      DB_HOST: ${DB_HOST}
       REDIS_URL: redis://redis:6379/0
+      DB_PORT: ${DB_PORT}
+      DB_USER: ${DB_USER}
+      DB_PASSWORD: ${DB_PASSWORD}
+      DB_NAME: ${DB_NAME}
     volumes:
       - ./cognito_ai_scdi_api/src:/app/src
       - ./cognito_ai_scdi_api/configs:/app/configs
@@ -137,7 +118,9 @@
     depends_on:
       redis:
         condition: service_started
-    networks: [task-net]
+      dbinit:
+        condition: service_completed_successfully
+    networks: [task-net, sdev-shared]
   ui:
     image: node:22
     restart: unless-stopped
@@ -189,8 +172,38 @@
       ui:
         condition: service_started
     networks: [task-net]
+  dbinit:
+    image: ${SDEV_POSTGRES_IMAGE}
+    restart: "no"
+    entrypoint:
+      - sh
+      - -ec
+    command:
+      - |
+        #!/bin/sh
+        # One-shot workspace database creation. Run in pgvector/postgres, never on host.
+        set -eu
+        case "$$DB_NAME" in ''|*[!a-z0-9_]*) echo 'invalid DB_NAME' >&2; exit 1 ;; esac
+        [ "$${#DB_NAME}" -le 63 ] || { echo 'DB_NAME exceeds 63 bytes' >&2; exit 1; }
+        # One session holds the advisory lock through existence check + CREATE. This
+        # tolerates two dbinit containers starting for the same workspace concurrently.
+        exec psql -v ON_ERROR_STOP=1 -v "db=$$DB_NAME" <<'SQL'
+        SELECT pg_advisory_lock(hashtextextended(:'db', 0));
+        SELECT format('CREATE DATABASE %I', :'db')
+        WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = :'db') \gexec
+        SELECT pg_advisory_unlock(hashtextextended(:'db', 0));
+        SQL
+    environment:
+      PGHOST: ${DB_HOST}
+      PGPORT: "5432"
+      PGUSER: sdev
+      PGPASSWORD: sdev
+      PGDATABASE: postgres
+      PGCONNECT_TIMEOUT: "10"
+      DB_NAME: ${DB_NAME}
+    networks:
+      - sdev-shared
 volumes:
-  scdi-postgres:
   scdi-ui-node-modules:
   npm-cache:
     external: true
@@ -198,3 +211,6 @@
 networks:
   task-net:
     driver: bridge
+  sdev-shared:
+    external: true
+    name: sdev-shared
--- a/core/projects.d/scdi.yml
+++ b/core/projects.d/scdi.yml
@@ -11,7 +11,7 @@
 default_shell_service: api
 template: core/scdi/docker-compose.tmpl
 # nginx host port = 5200 + offset (matches nginx.conf `listen 5200`).
-stack_services: [nginx, api, ui, db, redis]
+stack_services: [nginx, api, ui, redis]
 base_ports:
   nginx: 5200
 repos:
@@ -30,3 +30,5 @@
     default_base: develop
     compose_role: common
     link_node_modules: false
+infra:
+  postgres: "18"
